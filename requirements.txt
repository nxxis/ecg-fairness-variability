# Pinned where the paper specifies an exact version (Section III-D); unpinned
# otherwise. CPU-only installs of torch work fine for loading/evaluating the
# released checkpoints in checkpoints/; GPU + matching CUDA build is only
# needed to retrain from scratch.
numpy==2.1.3
scikit-learn==1.6.1
scipy
pandas
torch==2.11.0
wfdb
tqdm
matplotlib
